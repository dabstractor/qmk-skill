<!-- qmk-keycodes: version=0.24.6 semantic=true -->

# `0.24.6` — keycode changes

<sub>2024-03-22 · commit `4afbade6d1`</sub>

> One release in the QMK keycode migration catalog (`references/19-keycodes-changelog.md`). Apply the entries below only if your migration window `(from, to]` includes `0.24.6`.
>
> **Change-kind legend:** `deprecated OLD→NEW` → rename to **NEW** now (OLD still compiles via `quantum_keycodes_legacy.h`); `retired OLD` → replace with the **NEW** named where it was *deprecated* (OLD no longer compiles); `removed` → delete/substitute (no replacement); `added` → informational, not a migration requirement.

**Summary:** 1 layout(s) modified: spanish_latin_america; +1 keycode definitions

---

**Layout keycodes — `data/constants/keycodes/extras/` (DD source)** (diff vs `0.24.0`, 2024-02-28)  ·  +4 added / −0 removed
> 1 layout(s) modified: spanish_latin_america; +1 keycode definitions
```diff
diff --git a/data/constants/keycodes/extras/keycodes_spanish_latin_america_0.0.1.hjson b/data/constants/keycodes/extras/keycodes_spanish_latin_america_0.0.1.hjson
index 50c49274be..fb1de29e6e 100644
--- a/data/constants/keycodes/extras/keycodes_spanish_latin_america_0.0.1.hjson
+++ b/data/constants/keycodes/extras/keycodes_spanish_latin_america_0.0.1.hjson
@@ -335,5 +335,9 @@
             "key": "ES_CIRC",
             "label": "^",
         }
+        "ALGR(KC_NUHS)": {
+            "key": "ES_GRV",
+            "label": "`",
+        }
     }
 }
\ No newline at end of file
```
